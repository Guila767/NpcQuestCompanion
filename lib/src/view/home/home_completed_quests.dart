import 'package:flutter/widgets.dart';

class CompletedQuests extends StatefulWidget {
  const CompletedQuests({ Key? key }) : super(key: key);

  @override
  State<CompletedQuests> createState() => _CompletedQuestsState();
}

class _CompletedQuestsState extends State<CompletedQuests> {
  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}